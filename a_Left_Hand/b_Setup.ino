void setup () {
 pinMode   (LT,INPUT);
  pinMode  (M,INPUT);
   pinMode   (SR,INPUT); 
   pinMode (SL,INPUT);
   
    pinMode   (enA ,OUTPUT  );
     pinMode (enB,OUTPUT);
       pinMode (In1,OUTPUT);
         pinMode (In2,OUTPUT);
          pinMode (In3,OUTPUT);
            pinMode (In4,OUTPUT);
             pinMode (S2,OUTPUT);
              pinMode (S3,OUTPUT);
               pinMode (Out,INPUT);
 
         
         
   Serial.begin(9600);
}
